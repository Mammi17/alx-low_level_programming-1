#ifndef MAIN.H
#define MAIN.H
#include<stdio.h>

void positive_or_negative(int a);
int largest_number(int i, int b, int c);
void print_remaining_days(int mm, int dd, int aa);
int convert_day(int mm, int dd);

#endif
